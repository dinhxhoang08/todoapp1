version: '3.9'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/database:/app/app/database
      - ./.env:/app/.env
    environment:
      - DATABASE_URL=sqlite:///./app/database/todo.db
